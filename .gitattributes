# Auto-detect text files and perform LF normalization
* text=auto

# C# files (use CRLF in working copy on Windows)
*.cs text eol=crlf

# Angular / Web files (use LF everywhere)
*.ts text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.json text eol=lf

# Force LF line endings for markdown files
*.md text eol=lf

# Shell scripts
*.sh text eol=lf
